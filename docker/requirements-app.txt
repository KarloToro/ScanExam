# Dependencias de runtime de scanexam-app (entrenamiento P1 + pipeline P3 + panel stub P4).
# Subconjunto de la raíz requirements.txt (sin jupyter/ipython) + mlflow para versionado.
#
# Nota de compatibilidad: MLflow (todas las versiones disponibles) exige pandas<3,
# así que la imagen de app usa pandas 2.x (nuestro código P3/P4 no depende de
# ninguna API exclusiva de pandas 3). numpy queda sin pin para que pip resuelva
# una versión compatible entre torch, pandas y mlflow.

torch==2.13.0
torchvision==0.28.0
pillow==12.3.0
opencv-python-headless==5.0.0.93
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
pandas<3
openpyxl==3.1.5
Flask==3.1.3
Werkzeug==3.1.8

# MLOps / versionado del modelo
mlflow==3.14.0
